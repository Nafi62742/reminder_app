import 'package:flutter/material.dart';

/// Default theme — vivid blue, light.
class OceanTheme {
  OceanTheme._();

  static const String id = 'ocean';
  static const String label = 'Ocean';
  static const Color seedColor = Color(0xFF0077B6);
  static const Brightness brightness = Brightness.light;
}
